# التقرير الشامل لمشروع تحسين الشريط الجانبي (Sidebar)

## 1. فهم المشروع
موقع Portfolio متجاوب (HTML + CSS + JS نقي) يدعم PWA. التنقل يعمل عبر:
- نسخة سطح المكتب: `header > nav#hnv` (شريط علوي).
- نسخة الهاتف: `nav#navMobile` (شريط سفلي ثابت) يتم استنساخ الأزرار إليه من `#hnv` عبر JS.
- وضع الشريط الجانبي (Sidebar Mode): يُفعّل فقط عند ظهور زر Install App على الشاشات ≤768px، عبر `activateSidebarMode()` في `js.js`.

## 2. تحليل الجذر (Root Cause)
- أزرار الشريط الجانبي كبيرة (3em على ≤768px و2.5em على ≤425px) مما يجعل عرض الشريط ≈ 4.1em (~66px).
- الحل الحالي `body.sidebar-active section { padding-left: 7em }` يدفع كل الأقسام بعيداً (112px)، فيضيق محتوى الشاشات الضيقة ويسبب التداخل في الفقرة التعريفية والبطاقات.
- الحل: تصغير الشريط نفسه أبعاداً (أزرار + أيقونات + مسافات + توسيط) على غرار شريط YouTube للهواتف، ثم تقليص المساحة اليسرى إلى الحد الأدنى اللازم، ثم معالجة أي عنصر متداخل فردياً.

## 3. الملفات المتأثرة (لن تتغير الأسماء)
- `style.css` (تعديلات CSS فقط، كلها مقيدة بسياق `#navMobile.nav-sidebar` أو `body.sidebar-active`).
- لا تغيير على `index.html` ولا `js.js` ولا `manifest.json` ولا `sw.js`.

## 4. المحددات (Selectors) المستهدفة
- `#navMobile.nav-sidebar` (حاوية الشريط: عرض/حشوة/توسيط).
- `#navMobile.nav-sidebar .mqa` (قائمة الأزرار: تقليل الفجوات + التوسيط).
- `#navMobile.nav-sidebar .nvb` (الأزرار: تصغير الحجم مع الحفاظ على الشكل).
- `#navMobile.nav-sidebar .bimg` (الأيقونات: تصغير نسبي).
- `#navMobile.nav-sidebar .nvb:focus` (الحفاظ على تأثير Focus بدون قفزة الحجم 4em).
- `body.sidebar-active section` (تقليص padding-left من 7em إلى الحد الأدنى اللازم).
- أي عنصر متداخل فردي يُكتشف بالاختبار → يُضاف له أقل margin/padding فقط أثناء Sidebar Mode.

## 5. القيم المقترحة (قابلة للضبط بعد الاختبار الفعلي)
| الخاصية | الحالي | المقترح |
|---|---|---|
| حجم الزر ≤768px | 3em | ~2.4em |
| حجم الزر ≤425px | 2.5em | ~2.1em |
| حجم الأيقونة | 100% من الزر | ~62% من الزر |
| الفجوة بين الأزرار | 0.5em | ~0.3em |
| padding-left للشريط | 1em | ~0.4em |
| padding-left للأقسام | 7em | ~4em (يُضبط بالاختبار) |

## 6. تقسيم المهام (Micro-tasks)
1. تصغير أزرار الشريط الجانبي (`nvb`) مع الحفاظ على الشكل (≤768px و≤425px).
2. تصغير الأيقونات (`bimg`) بنسبة مناسبة داخل وضع الشريط فقط.
3. تقليل الفجوات بين الأزرار وإبقاء الأزرار في منتصف الشريط.
4. تقليص أبعاد حاوية الشريط (الحشوات) ليتقلص عرض الشريط.
5. إصلاح قفزة الحجم عند Focus داخل الشريط (مع بقاء التأثير البصري) والحفاظ على Hover.
6. تقليص المساحة اليسرى للأقسام (`padding-left: 7em → ~4em`).
7. اختبار فعلي بستة أحجام (768/600/425/400/375/320) وإضافة مساحة فردية طفيفة لأي عنصر متداخل فقط.
8. فحص نهائي شامل (أخطاء JS/CSS، Console، سطح المكتب، عودة الشريط السفلي عند اختفاء Install App، سلوك PWA).

## 7. آلية الاختبار الفعلي
- أداة سكرينشوت بدون واجهة (Headless Chrome) عبر `puppeteer-core` (مثبت في مجلد مؤقت خارج المشروع حتى لا نلوّث المستودع).
- تفعيل وضع الشريط برمجياً بنفس ما تفعله `activateSidebarMode()` (إضافة الكلاسين + زر Install) لأن حدث `beforeinstallprompt` لا يظهر في الوضع الصامت.
- التقاط لقطات لكل الأقسام والأحجام الستة + فحص `scrollWidth` لكشف أي تداخل أفقي.
- بعد كل تعديل: إعادة الاختبار فعلياً.

## 8. الالتزامات
- لا تغيير لأي ID أو Class أو اسم ملف أو عنصر HTML.
- لا تأثير على نسخة سطح المكتب.
- كل التعديلات تعمل فقط أثناء تفعيل Sidebar Mode.
- جميع وظائف JS وسلوك PWA باقية كما هي.
